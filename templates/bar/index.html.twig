{% extends 'base.html.twig' %}

{% block title %}Hello BarController!{% endblock %}

{% block body %}
<div class="row">
    {% if beers %}
				{% for beer in beers %}
					<div class="card">
						<div class="card-body">
							<h2 class="card-title">
                                <a href="{{ path('show_beer', { 'id' : beer.id }) }}">
                                    {{ beer.name }}
                                </a>
                            </h2>
						</div>
					</div>
				{% endfor %}
			{% else %}
				<p>Aucune bière</p>
			{% endif %}
</div>
{% endblock %}
